# خارطة طريق منظومة برنامج حُفّاظ السُّنة

**المنصّة:** React 19 + Vite (الواجهة) · Supabase (قاعدة البيانات، الدخول، الصلاحيات، الدوال الخادمية، التخزين)
**النشر الحالي:** الواجهة على GitHub Pages، وقاعدة البيانات على Supabase السحابي.
**آخر تحديث:** 2026-09-20

| المرحلة | الهدف | الحالة |
|---|---|---|
| 0. البناء الأساسي | تحويل النموذج الأولي إلى منظومة تعمل بقاعدة بيانات وصلاحيات حقيقية | ✅ منجزة |
| 1. النشر والتشغيل السحابي | Supabase سحابي + نشر آلي للواجهة + حسابات حقيقية | ✅ منجزة |
| 2. التحصين والجودة | RLS وصلاحيات الأعمدة، تحديد المعدل، اختبارات آلية، فحص استعمال المخطط | ✅ منجزة |
| 3. الإشعارات | طابور رسائل + تذكير قبل الموعد + تصدير للإرسال المحلي | 🟢 يعمل بالتصدير المحلي · ينقصه ربط مزود واتساب رسمي |
| 4. مواءمة اللوائح | الدليل الاسترشادي للتحكيم، المكاتب المعتمدة، المستويات، البرنامج المستمر | ✅ منجزة |
| 5. الشهادات والهوية | قالب PDF بإحداثيات حرة + شعار الجهة من الإعدادات | ✅ منجزة |
| 6. التشغيل الفعلي | بيانات حقيقية، تدريب، نسخ احتياطي، متابعة الأداء | ⏳ الخطوة التالية |
| 7. التوسعة | بوابة الطالب، المرفقات، تسجيل الجلسات، تقارير رسومية | لاحقاً |

---

## ما أُنجز حتى الآن

### قاعدة البيانات (12 ملف migration)
- **20 جدولاً** و**7 عروض** و**64 دالة**، مع ترقيم سنوي تلقائي: `STU-2026-00001` · `REG-` · `APT-` · `EX-` والشهادات ببادئة قابلة للتعديل.
- كل انتقالات الحالة تمر عبر إجراءات (RPC) تتحقق من الصلاحية والحالة وتكتب في `audit_log`.
- RLS على كل الجداول، وصلاحيات على مستوى الأعمدة، وحصر تنفيذ الدوال بأدوار محددة، وعروض `security_invoker`.
- احتساب الدرجة على الخادم مع **لقطة** من قواعد التقييم في `exams.config` وقت بدء الامتحان.
- تحديد معدل الطلبات العامة (60 استعلاماً و30 تسجيلاً لكل IP في 10 دقائق)، والموظفون مستثنون.

### آلية التحكيم (مطابقة للدليل الاسترشادي)
- 12 نوع خصم بكسور الدليل: ¼ (التلعثم، اللحن الخفي) · ½ (التردد، التقديم والتأخير، النقص والزيادة، التحلية والترضي والتعظيم) · 1 (التنبيه، الفتح، اللحن الجلي، الراوي أو التخريج).
- معيار «الصوت والأداء» من 10 بكسر مئوي (8.75) لتفادي التعادل.
- الحساب بأعداد صحيحة في الخادم والواجهة معاً، ويتحقق `npm run test:scoring` من تطابقهما على 600 حالة عشوائية.
- صفحة **«أساس التحكيم»** تجمع: المعايير · الخصميات · سلّم التقديرات · نص الدليل الاسترشادي، مع محاكاة مباشرة لأثر كل خصم.

### هيكلة البرنامج
- **بلا دورات:** البرنامج مستمر، والتسجيل يُفتح ويُغلق من الإعدادات.
- **المستويات من الأول إلى الخامس** بدل نظام «المتون» الملغى.
- **58 مكتب أوقاف معتمد**، مع استيراد دفعة واحدة من الإعدادات (لا حذف: إضافة وتحديث وتعطيل).
- **الطلبة السابقون:** التعرّف بالرقم الوطني وتاريخ الميلاد، تعبئة تلقائية، عرض المستويات السابقة، ومنع تكرار مستوى مجتاز، وطلب مفتوح واحد لكل طالب.

### الواجهة
- الموقع العام: الرئيسية · عن البرنامج · التسجيل (4 خطوات مع بطاقة «ممتحن سابقاً») · متابعة الطلب · الاستعلام عن النتيجة · التحقق من الشهادة.
- لوحة الإدارة (16 صفحة) ولوحة المحفّظ وقاعة الامتحان بحفظ تلقائي وتراجع.
- شهادة تُطبع فوق **قالب PDF أو صورة** بإحداثيات حرة (سحب الحقول)، أو بالتصميم المدمج.
- شعار الجهة وحجمه من الإعدادات، ويظهر فوراً في كل الواجهات.
- أسماء مستخدمين عربية (بريد Auth يُولَّد من بصمة الاسم)، وكلمة المرور التي يضعها الإداري نهائية.
- RTL كامل، وضع ليلي، واستجابة من 360 بكسل إلى الشاشات الكبيرة.

### الاختبارات (312 فحصاً في 6 مجموعات)
| المجموعة | تغطي |
|---|---|
| `test:db` | سير العمل كاملاً، الصلاحيات، RLS، إلغاء المتون والدورات، الطالب السابق، الاستيراد الجماعي للمكاتب |
| `test:scoring` | تطابق حساب الدرجة بين SQL و `scoring.js` + حالات الدليل |
| `test:notifications` | الطابور، التذكير، الإلغاء، التصدير للإرسال المحلي، الصلاحيات |
| `test:import` | قراءة CSV بصيغ Excel العربية ومطابقة الأسماء |
| `test:messaging` | مزودو الإرسال |
| `test:usage` | **فحص استعمال المخطط:** كل جدول وعمود وإجراء تستعمله الواجهة موجود فعلاً، ولا صلاحيات زائدة |
| `test:e2e` | Supabase حقيقي: Auth + PostgREST + RLS + Edge Function (يُشغَّل يدوياً) |

---

## المرحلة 6 — التشغيل الفعلي (الخطوة التالية)

### قبل استقبال أول دفعة حقيقية
- [ ] **حذف البيانات التجريبية من السحابة** (حالياً: 2 طالب · 3 طلبات · 2 امتحان · 2 شهادة · حسابات تجريبية).
- [ ] مراجعة **سلّم التقديرات** الافتراضي (ممتاز 90+ · جيد جداً 80 · جيد 70 · مقبول 60 · راسب دون ذلك) وتعديله إن لزم.
- [ ] مراجعة **الدرجة الأساسية للسؤال** في الإعدادات (كلما كبرت قلّ وزن خصميات الدليل؛ 20 يوازن كسور الدليل).
- [ ] رفع **شعار الجهة** و**قالب الشهادة** وضبط مواضع الحقول وطباعة نسخة تجريبية.
- [ ] إنشاء حسابات المحكّمين الحقيقيين وربط كل واحد بمكتبه.
- [ ] ضبط `public_site_url` في إعدادات الإشعارات ليعمل رابط المتابعة والتحقق داخل الرسائل.

### أثناء التشغيل
- [ ] تصدير الإشعارات المعلقة يومياً وإرسالها عبر النظام المحلي ثم «تحديد كمُرسلة».
- [ ] متابعة سجل العمليات ونتائج بانتظار الاعتماد من لوحة المتابعة.
- [ ] نسخة احتياطية أسبوعية (Supabase → Database → Backups، أو `supabase db dump`).

---

## المرحلة 7 — التوسعة (لاحقاً)

| البند | الوصف | الأثر |
|---|---|---|
| ربط مزود واتساب رسمي | WhatsApp Cloud API أو Twilio بدل التصدير اليدوي | إرسال آلي فوري |
| بوابة الطالب | دخول الطالب برقمه الوطني لمتابعة طلباته وشهاداته | تقليل الاستفسارات |
| مرفقات | صورة الهوية أو إثبات الحلقة مع الطلب | توثيق أدق |
| تسجيل الجلسات | حفظ رابط تسجيل الامتحان مع النتيجة | مرجع عند التظلم |
| تقارير رسومية | رسوم بيانية وتصدير Excel متقدم | قراءة أسرع للمؤشرات |
| تعدد اللجان | أكثر من محكّم لنفس الطالب ومتوسط الدرجات | حياد أعلى |

---

## قرارات مثبّتة (يمكن تغييرها من لوحة الإدارة)

| القرار | القيمة الحالية | أين تُعدَّل |
|---|---|---|
| الدرجة الأساسية للسؤال | حسب الإعدادات (المقياس الموصى به 20) | الإعدادات ← الامتحان |
| عدد أسئلة الجلسة | 3 | الإعدادات ← الامتحان |
| تجميع نتيجة الجلسة | متوسط الأسئلة | الإعدادات ← الامتحان |
| قيم الخصميات | كسور الدليل الاسترشادي | أساس التحكيم ← الخصميات |
| سلّم التقديرات | 5 نطاقات افتراضية | أساس التحكيم ← سلّم التقديرات |
| فتح التسجيل | مفتوح | الإعدادات ← الامتحان |
| المكاتب والمستويات | 58 مكتباً · 5 مستويات | الإعدادات ← القوائم المرجعية |
| قناة الإشعارات | تصدير محلي (لا مزود مربوط) | الإعدادات ← الإشعارات |

---

## ملاحظات تشغيلية

- **الترحيلات:** لا يُعدَّل ملف migration منشور؛ يُضاف ملف جديد باسم `YYYYMMDDHHMMSS_وصف.sql` ثم `npx supabase db push`.
- **تعديل قاعدة الدرجات:** يجب تعديل `compute_question_score`/`compute_exam_score` **و** `src/lib/scoring.js` معاً، ثم `npm run test:scoring`.
- **بعد إنشاء أي عرض جديد:** أعد ضبط الصلاحيات (مخطط `public` في Supabase يمنح anon/authenticated كل الصلاحيات افتراضياً) — يكشف ذلك `npm run test:usage`.
- **الأسرار:** `.env.local` و`.env.cloud.local` خارج Git، ومفتاح `service_role` لا يصل إلى المتصفح أبداً.
