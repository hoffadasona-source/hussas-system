# خارطة طريق منظومة برنامج حُفّاظ السُّنة

**المنصّة:** React + Vite (الواجهة) · Supabase (قاعدة البيانات، الدخول، الصلاحيات، الدوال الخادمية)
**نقطة البداية:** النموذج الأولي `hussas-system555.html` (واجهات ثابتة ببيانات تجريبية)
**الحالة الحالية:** المرحلة 0 منجزة، والجزء البرمجي من المرحلة 3 منجز، وأغلب بنود المرحلة 2 التي لا تحتاج خدمات خارجية منجزة. الخطوة التالية: ربط مشروع Supabase حقيقي والتجربة الميدانية.

| المرحلة | الهدف | المدة التقديرية | الحالة |
|---|---|---|---|
| 0. البناء الأساسي | تحويل النموذج إلى منظومة تعمل بقاعدة بيانات وصلاحيات حقيقية | — | ✅ منجزة |
| 1. الإطلاق التجريبي | تشغيل المنظومة على Supabase السحابي مع مكتب واحد | 2–3 أسابيع | التالية (أدوات الاستيراد والاختبار الشامل جاهزة) |
| 2. التحصين والجودة | الحماية من الإساءة، الاختبارات الآلية، المراقبة | 3 أسابيع | 🟡 جزئياً |
| 3. الإشعارات الآلية | واتساب/SMS/بريد من الخادم وتذكير قبل الموعد | 3–4 أسابيع | 🟡 البرمجة منجزة، ينقصها ربط مزود |
| 4. الشهادات والتقارير المتقدمة | PDF من الخادم، توقيع رقمي، Excel ولوحات بيانية | 3 أسابيع | |
| 5. التوسعة | بوابة الطالب، المرفقات، أنواع اختبارات متعددة، تسجيل الجلسات | 6–8 أسابيع | |
| 6. التشغيل والحوكمة | الخصوصية، النسخ الاحتياطي، التدريب، مؤشرات الأداء | مستمرة | |

---

## المرحلة 0 — البناء الأساسي ✅

### ما أُنجز

**قاعدة البيانات (5 ملفات migrations)**
- 20 جدولاً: الإعدادات، الدورات، المكاتب، المستويات، المتون، الحسابات، المحفّظون، الطلبة، الطلبات ومتونها، المواعيد، معايير التقييم، الخصميات، سلّم التقديرات، الامتحانات وأسئلتها، الشهادات، سجل العمليات، الإشعارات، عدّادات الترقيم.
- ترقيم تلقائي سنوي: `REG-2026-00001` · `STU-` · `APT-` · `EX-` · والشهادات ببادئة قابلة للتعديل.
- 21 إجراءً (RPC) تنفّذ كل انتقالات الحالة وتتحقق من الصلاحية والحالة الحالية وتسجّل في سجل العمليات وتُرسل الإشعارات.
- سياسات RLS لكل جدول + صلاحيات على مستوى الأعمدة + حصر تنفيذ الدوال، بحيث:
  - الزائر لا يقرأ أي بيانات شخصية مباشرة، ويصل فقط لإجراءات التسجيل والمتابعة والاستعلام والتحقق.
  - المحفّظ لا يرى إلا طلابه ومواعيده وامتحاناته، ولا يعدّل إلا أسئلة امتحان مفتوح يخصه.
  - لا أحد يغيّر حالة طلب أو امتحان أو درجة إلا عبر الإجراءات المعتمدة.
- احتساب الدرجة على الخادم مع لقطة من قواعد التقييم وقت بدء الامتحان.
- ست عروض (Views) للقوائم مع بحث وتصفية وترقيم صفحات من الخادم.
- تقرير شامل: حسب المكتب والمحفّظ والمستوى، ونسبة النجاح، والغياب، وأكثر الأخطاء تكراراً.

**الواجهة (React)**
- الموقع العام: الرئيسية، عن البرنامج، تسجيل من 4 خطوات مع تحقق وإيصال، متابعة الطلب بخط زمني، الاستعلام عن النتيجة بتفاصيل الأسئلة، التحقق من الشهادة مع مسح QR بالكاميرا.
- لوحة الإدارة (16 صفحة): المتابعة، الطلبات، الطلبة (ملف كامل بتبويبات وتعديل)، المحفّظون (مع إنشاء الحسابات)، الإحالات، المواعيد (مع واتساب)، الامتحانات، النتائج بانتظار الاعتماد، النتائج، الشهادات (معاينة وطباعة PDF وإلغاء)، معايير التقييم وسلّم التقديرات، الخصميات، التقارير، المستخدمون والصلاحيات، الإعدادات (الجهة، الامتحان، الشهادة، القوائم والدورات)، سجل العمليات.
- لوحة المحفّظ: المتابعة، طلابي، المواعيد، الامتحانات، نتائجي.
- قاعة الامتحان: معايير قابلة للتهيئة، بطاقات خصم بالعدد، تراجع متعدد الخطوات، حفظ تلقائي، تنبيه عند إغلاق الصفحة قبل الحفظ، تحذير عند الأسئلة غير المُقيَّمة، وضع عرض فقط بعد الإرسال، ودعم الإرجاع والتصحيح.
- نظام التصميم منقول من النموذج (الألوان، خط ثمانية، النقش)، مع الوضع الليلي، ودعم الهاتف، وتصدير CSV متوافق مع Excel، وطباعة.

**الخادم**
- Edge Function `manage-users`: إنشاء الحسابات، إعادة تعيين كلمة المرور بكلمة مؤقتة، الإيقاف والتفعيل، تعديل الدور والصلاحيات الفردية.
- سكربت `create-admin` لإنشاء أول مدير نظام.

**الاختبار**
- `npm test`: 141 فحصاً + 600 حالة عشوائية عبر خمس مجموعات (قاعدة البيانات، الإشعارات، الدرجات، الاستيراد، مزودو الإرسال)، تعمل على Postgres داخل الذاكرة دون خدمات خارجية.
- تجربة الواجهة في المتصفح بعرض الهاتف ببيانات تجريبية.

### قرارات اتُّخذت أثناء البناء (تحتاج تأكيد الإدارة)

| الموضوع | في النموذج | في المنظومة | السبب |
|---|---|---|---|
| درجة الصوت | إعدادات ثابتة (القصوى 10، المعامل 2) | «الصوت» معيار ضمن جدول المعايير بدرجة قصوى وافتراضية ومعامل خصم | يسمح بإضافة معايير أخرى (مثل الضبط أو المخارج) دون برمجة |
| الأساس ≠ 100 | غير محدد | النتيجة تُحوَّل نسبةً إلى 100 | لأن «الدرجة النهائية محصورة بين 0 و100» |
| حالات الطلب | 14 حالة بعضها متداخل | 11 حالة: دُمجت «معتمد نهائياً» في «منشور»، و«بانتظار الامتحان» و«امتحان منجز» في «قيد الامتحان»، و«معتمد من المحفّظ» في «بانتظار الإدارة» | منع الالتباس بين حالتين بنفس المعنى |
| حالات الامتحان | 7 حالات | 5: مسودة، جارٍ، بانتظار الإدارة، معتمد، مُعاد للمحفّظ | «مكتمل» و«اعتماد المحفّظ» و«بانتظار الإدارة» كانت خطوة واحدة فعلياً |
| الدخول | اسم مستخدم | اسم مستخدم يُحوَّل داخلياً إلى بريد وهمي `username@users.hussas.local` | Supabase Auth يعتمد البريد |
| تسجيل رقم وطني مسجَّل سابقاً | غير محدد | يُقبل الطلب الجديد فقط إن طابق الاسم وتاريخ الميلاد، ولا تُعدَّل بيانات الطالب من الموقع العام | منع تغيير بيانات طالب آخر بمعرفة رقمه الوطني |
| الاعتماد النهائي وإصدار الشهادات للإداري | «حسب الصلاحية» | صلاحيتان فرديتان يمنحهما مدير النظام لكل إداري | تطبيق مباشر لمصفوفة الصلاحيات |
| إعادة فتح نتيجة معتمدة | «إجراء إداري» | لمدير النظام فقط، بسبب مكتوب، ويُلغي الشهادة الصادرة تلقائياً | حماية النتائج المنشورة |
| الدورات | «الدورة الواحدة» مذكورة فقط | جدول دورات مع دورة حالية وفتح/إغلاق التسجيل | تطبيق شرط «طلب واحد لكل طالب في الدورة» |

---

## المرحلة 1 — الإطلاق التجريبي (2–3 أسابيع)

**الهدف:** تشغيل حقيقي مع مكتب واحد ومحفّظَين أو ثلاثة وعدد محدود من الطلبة.

### المهام
1. **إعداد Supabase السحابي**
   - إنشاء المشروع (منطقة Frankfurt)، تعطيل التسجيل العام في Auth، `db push`، نشر `manage-users`، ضبط الأسرار.
   - إنشاء حساب مدير النظام، وضبط بيانات الجهة والدورة والمكاتب والمتون من الإعدادات.
2. **النشر**
   - نشر الواجهة على Vercel أو Cloudflare Pages بنطاق رسمي (مثل `exams.hussas.ly`) مع HTTPS.
   - ضبط `Site URL` و `ALLOWED_ORIGIN`.
3. **اختبار شامل على البيئة الحقيقية** (لم يُنفَّذ بعد)
   - تشغيل `npm run test:e2e` (جاهز: 35 فحصاً للدخول الفعلي وإدارة الحسابات والمسار كاملاً عبر الـ API).
   - الدخول والخروج وانتهاء الجلسة، إنشاء الحسابات من Edge Function، وكل مسارات العمل بحسابات حقيقية.
   - اختبار قاعة الامتحان على هواتف المحفّظين الفعلية (Android/iPhone) وعلى اتصال ضعيف.
4. **تهيئة قواعد التقييم مع لجنة الامتحانات**
   - اعتماد قيم الخصميات ومعيار الصوت وعدد الأسئلة وطريقة التجميع.
   - **تحديد سلّم التقديرات ودرجة النجاح** (غير محدد في النموذج، وتتوقف عليه نسبة النجاح في التقارير والتقدير على الشهادة).
5. **إدخال البيانات الأولية:** المحفّظون وحساباتهم، وترحيل أي طلبات قائمة من جداول Excel إن وُجدت. ✅ أداة الاستيراد جاهزة: `npm run import` (فحص مسبق، مطابقة الأسماء، تقرير لكل صف).
6. **تدريب:** جلسة للإداريين (ساعة) وجلسة للمحفّظين على قاعة الامتحان (ساعة) مع دليل مختصر مصوّر.
7. **تجربة ميدانية** لمدة أسبوعين مع قناة لجمع الملاحظات.

### معايير القبول
- 20 طالباً على الأقل مرّوا بالمسار كاملاً حتى الشهادة دون تدخل تقني.
- لا توجد نتيجة ظهرت قبل اعتماد الإدارة، ولا وصول لبيانات خارج الصلاحية.
- زمن تحميل الصفحات أقل من 3 ثوانٍ على شبكة 4G محلية.

---

## المرحلة 2 — التحصين والجودة (3 أسابيع)

### المهام
1. **الحماية من الإساءة في الواجهات العامة**
   - Cloudflare Turnstile على نموذج التسجيل والاستعلامات، يُتحقق منه في Edge Function قبل تمرير الطلب.
   - ✅ تحديد معدل الطلبات لكل IP على التسجيل والاستعلامات والتحقق من الشهادات (مع استثناء الموظفين).
   - مراجعة ما يُعاد للزائر: الاكتفاء بالاسم مخفياً جزئياً في «متابعة الطلب» عند البحث بالرقم الوطني، أو اشتراط رقم الطلب مع الرقم الوطني معاً.
2. **أمان الحسابات**
   - ✅ إلزام تغيير كلمة المرور المؤقتة عند أول دخول، وحد أدنى 8 أحرف، وتغيير كلمة المرور من اللوحة.
   - المصادقة الثنائية (TOTP) لمدير النظام والإداريين أصحاب صلاحية الاعتماد.
   - تسجيل محاولات الدخول الفاشلة (Auth Hook) وقفل مؤقت بعد تكرارها.
3. **الاختبارات الآلية**
   - تحويل `tests/db.test.mjs` إلى اختبارات pgTAP تعمل مع `supabase test db`.
   - اختبارات واجهة شاملة بـ Playwright: التسجيل، الإحالة، الموعد، الامتحان، الاعتماد، الشهادة.
   - ✅ مطابقة `scoring.js` لدالة الخادم على 600 حالة عشوائية وحالات حدّية.
   - ✅ تشغيل الاختبارات تلقائياً في GitHub Actions مع كل تعديل (`.github/workflows/ci.yml`).
4. **المراقبة والأخطاء:** Sentry للواجهة، وتنبيهات من Supabase على أخطاء قاعدة البيانات والدوال.
5. **الأداء:** ✅ فهارس بحث نصي (`pg_trgm`) لأسماء الطلبة والأرقام الوطنية وأرقام الطلبات، وتحسين سياسات RLS (تقييم دوال الصلاحية مرة لكل استعلام). يبقى: مراجعة خطط الاستعلام عند تجاوز 10,000 طالب.
6. **مراجعة Supabase Security Advisor:** ✅ تثبيت `search_path` لكل الدوال، وحصر تنفيذ الدوال بالأدوار.
7. **إمكانية الوصول:** مراجعة التباين ولوحة المفاتيح وقارئ الشاشة بالعربية.

### معايير القبول
- تغطية اختبارات لكل انتقالات الحالة وكل سياسات RLS.
- صفر ثغرات حرجة في مراجعة أمنية مستقلة.

---

## المرحلة 3 — الإشعارات الآلية (3–4 أسابيع)

**الوضع الحالي:** ✅ البرمجة منجزة ومختبرة (41 فحصاً لقاعدة البيانات + 16 لمزودي الإرسال). ينقص: حساب WhatsApp Business واعتماد القوالب من Meta، أو مزود SMS، ثم تشغيل الجدولة. الإرسال اليدوي عبر واتساب يبقى متاحاً.

### المهام
1. ✅ **طابور الرسائل** (`outbound_messages`): بالانتظار، جارٍ الإرسال، أُرسلت، فشل، ملغاة؛ مع إعادة المحاولة مرتين، واستعادة الرسائل العالقة، ومنع التكرار.
2. ✅ **WhatsApp Cloud API** عبر Edge Function `send-messages`، مع محرر قوالب في الإعدادات (معاينة حية، متغيرات، ربط اسم قالب Meta وترتيب متغيراته) لكل من: استلام الطلب، قبوله أو رفضه، موعد الامتحان وتغييره، صدور النتيجة، صدور الشهادة.
   - ⏳ إنشاء القوالب في WhatsApp Manager وانتظار اعتمادها (إجراء إداري لدى Meta).
3. ✅ **التذكير التلقائي** قبل الموعد (افتراضياً 24 ساعة وساعتان، قابل للتعديل) بتوقيت ليبيا، ويُلغى عند تغيير الموعد أو الغياب. الجدولة عبر `pg_cron` في `supabase/snippets/schedule-send-messages.sql`.
4. ✅ **قنوات بديلة:** SMS عبر مزود HTTP عام (يحتاج تكييفاً بسيطاً مع المزود المعتمد)، وبريد إلكتروني عبر Resend.
5. ✅ صفحة **الرسائل** للإدارة (الحالة، إعادة الإرسال، الإلغاء، التصدير)، وتبويب الرسائل في ملف الطالب، ورسالة تجريبية للتأكد من الربط.
6. **تفضيلات الطالب** لقناة الإشعار (حالياً قناة واحدة للجميع تُختار من الإعدادات).
7. **الإشعارات الفورية للوحات** باستخدام Supabase Realtime بدل التحديث الدوري الحالي (كل دقيقة).

### معايير القبول
- 95% من الإشعارات تُسلَّم خلال دقيقة من الحدث.
- لا تُحفظ أي مفاتيح في الواجهة، وكلها في Supabase Secrets.

---

## المرحلة 4 — الشهادات والتقارير المتقدمة (3 أسابيع)

### المهام
1. **توليد PDF على الخادم** (Edge Function) بخط ثمانية، وحفظه في Supabase Storage، بدل طباعة المتصفح الحالية.
2. **ختم رقمي للشهادة:** بصمة SHA-256 لبيانات الشهادة تظهر في صفحة التحقق، مع توقيع وختم مصوّرين يُرفعان من الإعدادات.
3. **رفع الشعار وصورة التوقيع من الإعدادات** إلى Storage (حالياً الشعار ضمن ملفات المشروع).
4. **إصدار جماعي** للشهادات الجاهزة وتنزيلها ملفاً مضغوطاً.
5. **تقارير متقدمة:** رسوم بيانية زمنية، مقارنة الدورات، أداء كل محفّظ (متوسط الدرجات ونسبة الإرجاع)، توزيع الدرجات، وتصدير Excel حقيقي (xlsx) بتنسيق.
6. **تقرير رسمي قابل للطباعة** لكل دورة يُرفع للهيئة.

---

## المرحلة 5 — التوسعة (6–8 أسابيع)

1. **بوابة الطالب:** دخول برمز يُرسل على واتساب (OTP)، لعرض طلباته ومواعيده ونتائجه وشهاداته وتحميلها، وطلب تأجيل الموعد.
2. **الطلبة من خارج ليبيا:** قبول رقم جواز السفر بديلاً عن الرقم الوطني، مع التحقق من المفاتيح الدولية.
3. **المرفقات:** رفع صورة الهوية أو إفادة الحلقة عند التسجيل (Storage بسياسات خاصة).
4. **أنواع الاختبارات:** مرحلية ونصفية ونهائية، ولكل نوع قواعد تقييم وعدد أسئلة مستقل، وربط المستوى بمتون محددة.
5. **لجان الامتحان:** إمكانية إسناد الجلسة لأكثر من محكّم واحتساب متوسط تقييماتهم.
6. **تسجيل الجلسة الصوتية** (بموافقة الطالب) وحفظها للمراجعة عند الاعتراض.
7. **الاعتراض على النتيجة:** طلب مراجعة من الطالب بمهلة محددة، يصل للإدارة بسجل كامل.
8. **تطبيق PWA لقاعة الامتحان** يعمل عند انقطاع الإنترنت مؤقتاً ويزامن عند العودة.
9. **الربط مع تطبيق حُفّاظ السُّنة** الحالي لتوحيد بيانات الطلبة.

---

## المرحلة 6 — التشغيل والحوكمة (مستمرة)

- **النسخ الاحتياطي:** تفعيل Point-in-Time Recovery (خطة Supabase Pro)، ونسخة أسبوعية خارجية مشفرة، واختبار استرجاع كل ربع سنة.
- **الخصوصية:** سياسة خصوصية منشورة، وتحديد مدة الاحتفاظ بالبيانات، وحذف أو إخفاء بيانات الطلبات المرفوضة بعد مدة محددة.
- **سجل العمليات:** مراجعة شهرية لعمليات الاعتماد والإلغاء وإعادة فتح النتائج.
- **الصلاحيات:** مراجعة الحسابات الفعّالة كل دورة، وإيقاف حسابات المحفّظين غير النشطين.
- **مؤشرات الأداء:** زمن معالجة الطلب من التسجيل إلى الموعد، ونسبة الغياب، ونسبة النتائج المُرجعة، ومتوسط زمن الاعتماد.
- **التحديثات:** تحديث الحزم شهرياً، ومتابعة إصدارات Supabase CLI.

---

## المخاطر وخطط التعامل

| الخطر | الأثر | التعامل |
|---|---|---|
| ضعف الإنترنت أثناء الجلسة | ضياع تقييمات | حفظ تلقائي بعد كل نقرة، وتنبيه عند فشل الحفظ مع زر إعادة المحاولة، ثم PWA في المرحلة 5 |
| تسجيلات وهمية أو إغراق النموذج | تشويش البيانات | Turnstile وتحديد المعدل (المرحلة 2) |
| تسريب بيانات الطلبة | ضرر قانوني وسمعة | RLS مطبّق ومختبر، وتقليل ما يُعرض للزائر، ومصادقة ثنائية للإدارة |
| عدم اعتماد سلّم التقديرات قبل الإطلاق | شهادات بلا تقدير، ونسبة نجاح غير محسوبة | بند إلزامي في المرحلة 1 |
| تغيير قواعد التقييم أثناء الدورة | اختلاف معاملة الطلبة | لقطة القواعد داخل كل امتحان (منجز)، وتوصية بتثبيت القواعد طوال الدورة |
| الاعتماد على شخص واحد لإدارة النظام | توقف العمل | حسابا «مدير نظام» على الأقل، وتوثيق التشغيل |

## مؤشرات نجاح الإطلاق
- استبدال كامل لجداول Excel والمراسلات اليدوية في مكتب التجربة.
- تقليل زمن إصدار الشهادة من الامتحان إلى أقل من 3 أيام عمل.
- رضا المحفّظين عن قاعة الامتحان (استبيان) بنسبة 80% أو أكثر.
